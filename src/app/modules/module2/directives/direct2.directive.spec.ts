import { Direct2Directive } from './direct2.directive';

describe('Direct2Directive', () => {
  it('should create an instance', () => {
    const directive = new Direct2Directive();
    expect(directive).toBeTruthy();
  });
});
