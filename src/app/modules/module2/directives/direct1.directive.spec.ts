import { Direct1Directive } from './direct1.directive';

describe('Direct1Directive', () => {
  it('should create an instance', () => {
    const directive = new Direct1Directive();
    expect(directive).toBeTruthy();
  });
});
